# C++ 综合编程题：学生成绩管理系统

## 题目描述

设计一个**学生课程成绩管理系统**，使用面向对象编程，支持学生信息管理、成绩录入、查询排序、统计和文件存储。

## 工作要求

1. **Person 抽象基类**：包含学号、姓名等共性信息，声明纯虚函数用于多态展示信息。
2. **Student 类**：继承 Person，包含专业（用枚举定义）、入学年份、各科成绩（用动态数组存）、能计算平均分、支持深拷贝。
3. **Teacher 类**：多继承，既是 Person 也是一个 Evaluator 评分接口，能给 Student 打分。
4. **TeachingAssistant 类**：继承 Student 和 Evaluator，需用虚基类解决菱形继承问题。
5. **类模板 ScoreList\<T\>**：泛型容器，支持添加、下标访问、排序，需支持深拷贝和赋值。
6. **多态**：基类指针数组统一管理 Student 和 Teacher，通过虚函数实现运行时多态。
7. **运算符重载**：Student 类重载 `>` `<` `==` 按平均分比较，重载 `<<` 用友元函数输出对象信息，实现类型转换函数返回平均分。
8. **静态成员**：统计系统中总人数。
9. **const 成员函数**：恰当的 getter 函数应标记为 const。
10. **函数重载**：至少实现 3 个版本求最大值函数（参数个数不同）。
11. **带默认参数的函数**：计算平均分的函数支持是否四舍五入的选项。
12. **内联函数**：实现判断成绩是否及格的函数。
13. **函数模板**：实现交换任意类型两个变量的函数。
14. **this 指针**：Student 的 setter 方法返回 `*this` 以实现链式调用。
15. **友元**：Teacher 作为 Student 的友元类可访问私有数据；`<<` 重载使用友元函数。
16. **预处理**：`#define` 定义学校名称，`#ifdef` 控制调试输出。
17. **文件 I/O**：支持文本文件保存/读取学生数据，支持二进制文件读写，支持带格式导出成绩报表（用 setw、setprecision 等操纵子）。
18. **主程序**：switch 菜单系统（添加/录入成绩/查询/排序/统计/保存/读取/导出/退出），使用 new/delete 动态创建对象，程序退出前无内存泄漏。

## 提交要求

- 所有代码写在一个 `.cpp` 文件中或合理分文件
- 代码编译无警告（`g++ -Wall -Wextra`）
- 运行无内存泄漏
